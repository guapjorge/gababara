<section class="container2">
    <div class="row">
        {{#each jewelry as |item|}}
        <div class="col s12 m7">
            <div class="card">
                <div class="card-image">
                    <img src="{{item.image}}">
                    <span class="card-title">{{item.jewelryName}}</span>
                </div>
                <div class="card-content">
                    <p>${{item.jewelryPrice}}</p>
                </div>
                <div class="card-action">
                    <a href="#">This is a link</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    adfasdafa

</section>